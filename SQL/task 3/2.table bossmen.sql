CREATE TABLE IF NOT EXISTS bossmen (
id INT UNSIGNED NOT NULL AUTO_INCREMENT primary key,
first_name VARCHAR(30) NOT NULL,
last_name VARCHAR(30) NOT NULL,
dolzhnost varchar(30) not null,
salary int (30)
);
